<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #666;
            font-weight: bold;
        }

        #todo-container {
            background-color: white;
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #333;
            text-align: center;
        }

        .add-todo {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .add-todo input[type="text"],
        .add-todo input[type="date"],
        .add-todo textarea,
        .add-todo select {
            padding: 10px;
            margin-right: 10px;
            border: 2px solid #ddd;
            border-radius: 4px;
            width: 100%;
        }

        textarea {
            resize: vertical;
        }

        button {
            padding: 5px 10px;
            background-color: #5cb85c;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #4cae4c;
        }

        #list-container ul {
            list-style-type: none;
            padding: 0;
        }

        #list-container ul li {
            padding: 10px;
            background-color: #eee;
            border-bottom: 1px solid #ddd;
            margin-top: 5px;
        }

        #list-container ul li:last-child {
            border-bottom: none;
        }


        #todo-container {
            max-width: 700px;
            margin: 50px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        #list-container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        h3 {
            font-size: 20px;
            font-weight: 500;
            color: #34495e;
            /* Dark shade of blue */
            background-color: #ecf0f1;
            /* Light grey background */
            text-align: center;
            border-radius: 10px;
            transition: transform 0.3s ease-in-out, color 0.3s ease;
            cursor: pointer;
        }

        h3:hover {
            transform: scale(1.1);
            /* Enlarges the text */
            color: #e74c3c;
            /* Changes color to red */
        }



        .add-todo {
            display: flex;
            margin-top: 20px;
        }

        .add-todo input[type="text"] {
            flex: 1;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 3px;
            margin-right: 10px;
        }

        .add-todo button {
            padding: 10px;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }

        .content {
            flex: 1;
            padding: 10px 20px;
            /* margin-right: 3000px; */
        }

        .buttons {
            display: flex;
            gap: 10px;
        }

        .buttons button {
            padding: 5px 10px;
            border: none;
            border-radius: 2px;
            cursor: pointer;
            /* display: none; */
        }

        .buttons button:hover {
            border-radius: 5px;
            transition-duration: 1s;
            background: transparent;
            color: black;
            outline: 1px solid black;
        }

        .done {
            background-color: #0a6107;
            color: #fff;
        }

        .update {
            background-color: #3498db;
            color: #fff;
        }

        p {
            font-size: 10px;
            transition: transform 0.3s ease-in-out, color 0.3s ease;
        }

        p:hover {
            transform: scale(1.1);
            color: rgb(21, 21, 120);
        }
    </style>

</head>

<body>

    <div id="todo-container">
        <h1>Poco Loco Tasker</h1>

        <div class="add-todo">
            <label for="title">Task Title:</label>
            <input type="text" id="title" placeholder="Enter task title">

            <label for="description">Description:</label>
            <textarea id="description" placeholder="Describe your task"></textarea>

            <label for="priority">Priority:</label>
            <select id="priority">
                <option value="low">Low</option>
                <option value="medium">Medium</option>
                <option value="high">High</option>
            </select>

            <label for="dueDate">Due Date:</label>
            <input type="date" id="dueDate">

            <label for="tags">Tags:</label>
            <input type="text" id="tags" placeholder="Enter tags, separated by commas">

            <button id="todoCreateBtn">Create Task</button>
        </div>

        <div id="list-container">
            <ul id="created-task">
            </ul>
        </div>

        <div id="list-done-container">
            <h3>Completed Task</h3>
            <ul id="completed-task">
            </ul>
        </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.7.1.js"
        integrity="sha256-eKhayi8LEQwp4NKxN+CfCh+3qOVUtJn3QNZ0TciWLP4=" crossorigin="anonymous"></script>

    <script>

        function toggleDetails(taskId) {
            const moreInfo = document.querySelector(`#task${taskId} .more-info`);
            const button = document.querySelector(`#task${taskId} .view-more`);
            if (moreInfo.style.display === "none") {
                moreInfo.style.display = "block";
                button.textContent = "View Less";
            } else {
                moreInfo.style.display = "none";
                button.textContent = "View More";
            }
        }

        $(document).ready(function () {
            //ajax call the get all the task from the backend and list it

            getAllTasks();

            //get a call to create a new task
            function getAllTasks() {
                $.ajax({
                    type: "GET",
                    url: "http://localhost:3000/api/getAllTasks",
                    success: function (data) {
                        displayTodoList(data)
                    },
                    error: function (err) {
                        console.log(err);
                    }
                });
            }


            function displayTodoList(tasks) {
                let htmStr = "";
                let count = 0;

                tasks.forEach((element) => {
                    htmStr += `<li id="task${count}" name="${element._id}">
                    <div class="content">
                        <h3>${element['title']}</h3>
                        <p>${element['description']}</p>
                        <div class="more-info" style="display: none;">
                            <p><strong>Priority:</strong> ${element['priority']}</p>
                            <p><strong>Due Date:</strong> ${element['dueDate']}</p>
                            <p><strong>Tags:</strong> ${element['tags'].join(', ')}</p>
                        </div>
                    </div>
                    <div class="buttons">
                        <button class="view-more" onclick="toggleDetails(${count})">View More</button>
                        <button class="done" name="done" id=${count}>Done</button>
                        <button class="update">Update</button>
                    </div> 
                     </li>`;
                    count++;
                });

                $("#created-task").html(htmStr);

                $(".done").click(function (e) {
                    e.preventDefault();
                    var parentId = $(this).parent().parent().attr('id');
                    var taskId = $(this).parent().parent().attr('name');

                    $.ajax({
                        type: "DELETE",
                        url: "http://localhost:3000/api/done",
                        data: JSON.stringify({
                            taskId: taskId
                        }), // Ensure the data is a stringified JSON object
                        contentType: "application/json", // Add contentType to specify the type of data being sent
                        dataType: "json", // Expected response type from the server
                        success: function (response) {
                            console.log(response);
                        },
                        error: function (err) {
                            console.log(err);
                        }
                    });

                    $("#" + parentId).remove();
                });
            }




            document.getElementById('todoCreateBtn').addEventListener('click', function () {
                var title = document.getElementById('title').value.trim();
                var description = document.getElementById('description').value.trim();
                var priority = document.getElementById('priority').value;
                var dueDate = document.getElementById('dueDate').value;
                var tags = document.getElementById('tags').value.split(',').map(tag => tag.trim()).filter(tag => tag !== '');

                let createTaskData = {
                    "title": title,
                    "description": description,
                    "priority": priority,
                    "dueDate": dueDate,
                    "tags": tags,
                };

                //send the create task request to the server
                $.ajax({
                    type: "POST",
                    url: "http://localhost:3000/api/createTask",
                    data: JSON.stringify(createTaskData), // Ensure the data is a stringified JSON object
                    contentType: "application/json", // Add contentType to specify the type of data being sent
                    dataType: "json", // Expected response type from the server
                    success: function (response) {
                        console.log(response);
                        getAllTasks();
                    },
                    error: function (err) {
                        console.log(err);
                    }
                });
            });
        });


    </script>

</html>